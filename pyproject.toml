[project]
name = "polymind"
version = "0.1.0"
description = "AI-for-polymer-materials tracker"
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.40.0",
  "jinja2>=3.1.0",
  "pyyaml>=6.0.0",
  "voyageai>=0.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

